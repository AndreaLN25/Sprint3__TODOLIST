<!DOCTYPE html>
<html>

<head>
<title>All Tasks</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th,
        td {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
        }
        a.button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-right: 10px;
        }

        a.button:hover {
            background-color: #0056b3;
        }
        .no-tasks {
            margin-top: 20px;
            padding: 10px;
            background-color: #f2f2f2;
            border-radius: 5px;
        }
    </style>    
</head>

<body>
    <?php
    //var_dump($this->allTasks);
    if (isset($this->allTasks['tasks']) && count($this->allTasks['tasks']) > 0) : ?>
        <table>
            <thead>
                <tr>
                    <th>Task ID</th>
                    <th>Description</th>
                    <th>Status</th>
                    <th>Priority</th>
                    <th>Assigned to</th>
                    <th>Creation Date</th>
                    <th>Deadline</th>
                    <th>Updated At</th>
                    <th>Created By</th>
                    <th>Updated By</th>

                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->allTasks['tasks'] as $task) : ?>
                    <tr>
                        <td><?php echo isset($task['task_id']) ? $task['task_id'] : ''; ?></td>
                        <td><?php echo isset($task['task_description']) ? $task['task_description'] : ''; ?></td>
                        <td><?php echo isset($task['task_status']) ? $task['task_status'] : ''; ?></td>
                        <td><?php echo isset($task['task_priority']) ? $task['task_priority'] : ''; ?></td>
                        <td><?php echo isset($task['task_assigned_to']) ? $task['task_assigned_to'] : ''; ?></td>
                        <td><?php echo isset($task['task_creation_date']) ? $task['task_creation_date'] : ''; ?></td>
                        <td><?php echo isset($task['task_deadline']) ? $task['task_deadline'] : ''; ?></td>
                        <td><?php echo isset($task['task_updated_at']) ? $task['task_updated_at'] : ''; ?></td>
                        <td><?php echo isset($task['task_created_by']) ? $task['task_created_by'] : ''; ?></td>
                        <td><?php echo isset($task['task_updated_by']) ? $task['task_updated_by'] : ''; ?></td>

                        <td>
                            <form method="post" action="<?= WEB_ROOT ?>/editTask">
                                <input type="hidden" name="task_id" value="<?php echo $task['task_id']; ?>">
                                <button type="submit">Editar</button>
                            </form>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <a href="<?= WEB_ROOT ?>/createTask" class="button">Create Task</a>
        <a href="<?= WEB_ROOT ?>/getTaskById" class="button">Search Task By Id</a>
        <a href="<?php echo WEB_ROOT; ?>/logout" class="button">Log out</a>
    <?php else : ?>
        <div class="no-tasks">
            <p>No tasks available :( ).</p>
        </div>
    <?php endif; ?>
</body>

</html>