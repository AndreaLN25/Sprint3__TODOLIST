<!DOCTYPE html>
<html>
<head>
    <title>All Tasks</title>
</head>
<body>
    <?php
    //var_dump($this->allTasks);
    if (isset($this->allTasks['tasks'])): ?>
        <table>
            <thead>
                <tr>
                    <th>Task ID</th>
                    <th>Description</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->allTasks['tasks'] as $task) : ?>
                    <tr>
                        <td><?php echo $task['task_id']; ?></td>
                        <td><?php echo $task['task_description']; ?></td>
                        <td><?php echo $task['task_status']; ?></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <a href="/createTask">Create Task</a>
    <?php else : ?>
        <p>No tasks available.</p>
    <?php endif; ?>
</body>
</html>
